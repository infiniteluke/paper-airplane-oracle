# Anthropic API key — https://platform.anthropic.com → Settings → API Keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional shared passcode. If set, /api/oracle requires the matching
# x-access-code header. Users enter it once on the gate screen and the
# client persists it in localStorage. Leave blank to disable the gate.
ACCESS_CODE=
